var calc = {};

calc.add = (a, b) => {
    return a + b;
}

calc.multiply = (a, b) => {
    return a * b;
}

const sum = (a, b) => {
    return a + b;
}

const asd = '문자열';

module.exports = {
    calc,
    sum,
    asd
};